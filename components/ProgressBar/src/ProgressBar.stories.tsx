import React from 'react';

import ProgressBar from '.';
import notes from '../README.md';

export default {
  title: 'Components|Progress Bar',
  parameters: { notes }
};

export const BasicUsage = () => <ProgressBar />;
